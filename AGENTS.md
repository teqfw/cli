# Root Level

- Path: `AGENTS.md`
- Changed: `20260726`

## Purpose

This repository contains the `@teqfw/cli` software product and its ADSM cognitive context in `ctx/`.

## Project Model

The cognitive context is the authoritative project knowledge.
Read `ctx/AGENTS.md` and `ctx/docs/filesystem.md` before changing the product.
Maintain the dependency order product → architecture → environment → code.

## Human And Agent Roles

The Human owns goals, accepted meaning, approval, and accountability.
Agents implement authorized work, keep context and product aligned, and expose decisions beyond the accepted scope.

## AGENTS.md Hierarchy

Apply every `AGENTS.md` from the repository root to the target path.
Deeper instructions refine but do not contradict this file.

## Root File Protection

Do not modify, replace, delete, relocate, or reinterpret this file without explicit Human authorization.
