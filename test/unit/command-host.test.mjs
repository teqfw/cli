import assert from 'node:assert/strict'; import test from 'node:test'; import Host from '../../src/Host.mjs'; import Parser from '../../src/Adapter/Parser/Internal.mjs';
const io = {write() {}, error() {}}; const signals = {subscribe() { return () => {}; }};
test('host runs a finite command and reverses lifecycle work', async () => { const calls = []; const command = {id: 'x:run', path: ['x'], summary: 'x', lifetime: 'finite', arguments: [], options: [], execute: async () => calls.push('run')}; const participant = {id: 'p', initialize: async () => calls.push('init'), activate: async () => calls.push('activate'), deactivate: async () => calls.push('deactivate'), dispose: async () => calls.push('dispose')}; const status = await new Host({parser: new Parser(), signals, io}).run({argv: ['node', 'teq', 'x'], version: '1', commands: [command], participants: [participant]}); assert.equal(status, 0); assert.deepEqual(calls, ['init', 'activate', 'run', 'deactivate', 'dispose']); });
test('host rejects malformed long-running command handles', async () => { const command = {id: 'x:run', path: ['x'], summary: 'x', lifetime: 'long-running', arguments: [], options: [], start: async () => ({})}; const status = await new Host({parser: new Parser(), signals, io}).run({argv: ['node', 'teq', 'x'], version: '1', commands: [command], participants: []}); assert.equal(status, 1); });
